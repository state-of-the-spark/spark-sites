# NEXT (first thing 2026-08-13): hand New Home to Claude Design to actually design it

**Plan:** Take the Spark Sites **New Home** page (`sparkmysite.com/new-home/`, built here as `index-quiet-voltage.html`) and give it to **Claude Design** to properly design/refine it *in Claude Design* — this build is a good starting point, not the finished design. Then re-sync the improved version back per `grant-sparks/reference/systems/claude-design-sync.md`.

**Grant's feedback (2026-08-12) — what to improve:**
- **It's a good start**, but it's **too plain** overall. Push it further.
- **More color.** The brand has more color throughout than this page uses — bring more of it in. (Right now it's very restrained: porcelain/ink + a rationed cyan hairline. Grant wants more of the brand's color in play across the page.)
- **Keep the starkness** — Grant *likes* how stark/high-contrast it is. So: add color and richness **without** losing the bold, stark quality.
- **Improve the information** — the content/copy and info hierarchy need work.
- **Typography** — font sizes and type scale/hierarchy need to be dealt with.
- **Background images** — the page needs some; add background imagery (it currently has none, which is part of why it reads plain).

**Net:** stark + bold is right; plain is not. Add color, real imagery/backgrounds, stronger information, and a proper type scale — done in Claude Design, then synced back to the repo and re-pushed to `/new-home/`.

**Handoff inputs for Claude Design:** the current page (`index-quiet-voltage.html`), the Quiet Voltage brand tokens (`spark-sites/core/brand/quiet-voltage.md` + `tokens/spark-sites.css`), and the master kit (`grant-sparks/reference/brand/spark-brand-system/`). Note the parent "Live Wire" palette has more color (cyan + magenta) to borrow from for "more color" — but keep Spark Sites' discipline; let Claude Design balance.
